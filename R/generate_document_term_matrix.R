#' A function to generate a document term matri from a list of document term vectors.
#'
#' @param document_word_list A list of term vectors, one per document, that we wish to turn into a document term matrix.
#' @param vocabulary An optional vocabulary vector which will be used to form the document term matrix. Defaults to NULL, in which case a vocabulary vector will be generated internally.
#' @return A list object.
#' @export
generate_document_term_matrix <- function(document_word_list,
                                          vocabulary = NULL){



}
