#' A function to generate a document term vector list in a standard format and save it to file.
#'
#' @param input A list of term vectors, one per document, that we wish to save.
#' @param return_result A logical value indicating whether the resulting list object should be returned.
#' @return A document term vector list.
#' @export
generate_document_term_vector_list <- function(input,
                                               return_result = FALSE){

    return(NULL)
}
